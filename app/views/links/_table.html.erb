<p>
   <%= link_to "Add new link!", new_link_path, class: "btn btn-medium btn-primary" %>
</p>
<table>
	<% @links.each do |link| %>
		<tr>	
			<th><%= link_to link.title, link.url, :target => "_blank" %></th>
  		<td><%= link.description %></td>
			<td><%= link.user.name %></td>
		</tr>
	<% end %>
</table>