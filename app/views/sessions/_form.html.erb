<%= form_for(:session, url: sessions_path) do |f| %>
	<%= render 'shared/error_messages', :obj => :session %>
	
  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>
  <br/>
  <%= f.submit "Sign in", class: "btn btn-large btn-primary" %>
<% end %>